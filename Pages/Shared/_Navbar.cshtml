<nav class="fixed w-full z-50 bg-white/95 backdrop-blur-md border-b border-gray-100 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">

            <!-- Logo -->
            <a href="#" class="flex items-center gap-3 group">
                <div class="relative flex items-center justify-center w-12 h-12 bg-brand-black rounded-lg border-2 border-brand-black group-hover:bg-brand-dark transition-colors overflow-hidden">
                    <div class="absolute left-0 top-2 w-full h-0.5 bg-brand-gold/30"></div>
                    <div class="absolute left-0 top-4 w-full h-0.5 bg-brand-gold/30"></div>
                    <div class="absolute left-0 top-6 w-full h-0.5 bg-brand-gold/30"></div>
                    <span class="text-brand-gold font-black text-3xl font-heading relative z-10 pt-1">T</span>
                </div>
                <div class="flex flex-col justify-center">
                    <span class="text-[28px] font-black tracking-tight leading-none text-brand-black font-heading">ECH99</span>
                    <span class="text-[0.6rem] tracking-[0.2em] text-brand-gold font-bold uppercase mt-1">Tech Repair &amp; Sales</span>
                </div>
            </a>

            <!-- Desktop Links -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="#services" class="text-sm font-semibold text-gray-600 hover:text-brand-gold transition-colors">Services</a>
                <a href="#shop" class="text-sm font-semibold text-gray-600 hover:text-brand-gold transition-colors">Preowned Shop</a>
                <a href="#about" class="text-sm font-semibold text-gray-600 hover:text-brand-gold transition-colors">About Us</a>
            </div>

            <!-- Desktop CTA -->
            <div class="hidden md:flex items-center gap-4">
                <div class="flex items-center gap-2 text-sm font-medium text-gray-600 border-r border-gray-200 pr-4">
                    <i class="ph-fill ph-map-pin text-brand-gold text-lg"></i>
                    Davao City
                </div>
                <a href="#contact" class="bg-brand-black text-white px-6 py-2.5 rounded-md text-sm font-bold hover:bg-brand-gold transition-colors duration-300 flex items-center gap-2">
                    <i class="ph-bold ph-wrench"></i>
                    Book Repair
                </a>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center">
                <button class="text-brand-black hover:text-brand-gold">
                    <i class="ph ph-list text-3xl"></i>
                </button>
            </div>
        </div>
    </div>
</nav>
